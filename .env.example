# Copy to .env (gitignored), then load with:  set -a; source .env; set +a
# No secrets belong here: local runs authenticate with `gcloud auth application-default login`.
GCP_PROJECT_ID=your-gcp-project-id
LAKE_ROOT=gs://your-gcp-project-id-lake
