name: nyc_taxi
version: "0.1.0"
profile: nyc_taxi

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
clean-targets: ["target", "dbt_packages"]

models:
  nyc_taxi:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
